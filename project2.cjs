let invent = require('./cars');
let inventory = invent.inventory;
findByLastId(inventory);
function findByLastId(inventory) {

    let obj = inventory[inventory.length-1];
    console.log('Lst car is a', );
    // console.log('Last car is a', obj.car make goes here* *car model goes here*)
}